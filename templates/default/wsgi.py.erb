import os
import sys
import site

# Add the app's directory to the PYTHONPATH
sys.path.append('<%= node['rattic']['apache2']['doc_root'] %>')

os.environ['DJANGO_SETTINGS_MODULE'] = 'ratticweb.settings'

import django.core.handlers.wsgi
application = django.core.handlers.wsgi.WSGIHandler()